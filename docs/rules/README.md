# 规则细则索引

- `retrieval.md`：检索范围与扩大条件。
- `comments.md`：注释写法边界。
- `styling.md`：Tailwind 与 `.scss` 分工。
- `types.md`：公共类型文件组织。
- `nextjs.md`：Next.js 本地文档检索流程。

## 已迁移到 skills 的规则

- `.agents/skills/encoding-guard/SKILL.md`：替代原 `encoding.md`。
- `.agents/skills/component-reuse-guard/SKILL.md`：替代原 `component-reuse.md`。
- `.agents/skills/task-completion-guard/SKILL.md` 与 `.agents/skills/delivery-reporting/SKILL.md`：替代原 `change-process.md`。
- `.agents/skills/INDEX.md` 与 `.agents/skills/skill-usage-disclosure/SKILL.md`：替代原 `skills.md`。
